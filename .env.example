# Copy this file to .env and fill in real values for local (non-Docker) setup.
# When using Docker Compose, these are already set in docker-compose.yml.

DB_HOST=localhost
DB_NAME=electroshop
DB_USER=root
DB_PASS=
